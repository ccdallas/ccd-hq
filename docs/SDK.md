# CCD Platform SDK™ Reference

The SDK (`src/sdk/index.js`) serves as the single source of truth for creating nodes, linking relationships, emitting system events, and calculating mission health scores.
